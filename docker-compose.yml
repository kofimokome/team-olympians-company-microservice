version: '3.3'

services:

  flask:
    container_name: flask
    build:
      context: .
      dockerfile: Dockerfile
#    volumes:
#      - '.:/app'
#      - '/app/node_modules'
    restart: 'always'
    ports:
      - "${FLASK_PORT:-5000}:5000"
    env_file:
     - .env
